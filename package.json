{
  "scripts": {
    "build": "tsc",
    "test": "npm run build && playwright test",
    "clean": "rm -rf allure-report allure-results test-results storageState.json",
    "clean:storage": "rm -rf storageState.json",
    "test:dev": "npm run clean && ENVIRONMENT=dev npx playwright test",
    "test:stage": "npm run clean && ENVIRONMENT=stage npx playwright test",
    "allure:generate": "allure generate ./allure-results --clean -o ./allure-report",
    "allure:open": "allure open ./allure-report",
    "test:report": "npm run test && npm run allure:generate && npm run allure:open"
  }
}
